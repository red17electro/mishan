{
  "name": "mishan",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "strip-uploads": "node scripts/strip-upload-metadata.mjs",
    "check-uploads": "node scripts/strip-upload-metadata.mjs --check"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "astro": "latest",
    "sharp": "^0.34.3",
    "typescript": "latest",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8"
  }
}
